# PINN_WE 工作记录 (2025.01 - 2026.02)

---

## 1. Self-Discovery: Guderley 自相似指数自发现

### 目标
不给定自相似指数 α 的值，让 PINN 从 Guderley 激波内爆方程中自动"涌现"出 α ≈ 0.717。

### 已完成工作

**基础框架搭建** (`cases/self-discovery/src/`)
- `model.py`: 网络结构，4层×64神经元，输入 ξ 输出 (V, C, G)
- `physics.py`: Guderley 自相似 ODE，硬约束边界条件（Rankine-Hugoniot）
- `train.py` / `train_improved.py`: 两阶段训练（Adam + LBFGS）
- α 作为可学习参数，log空间存储，sigmoid映射到 [0.5, 1.0]

**α 扫描实验** (v1 ~ v9，共9个版本迭代)
- 固定不同 α 值训练网络，观察 PDE loss 随 α 的变化
- 目标：验证 loss landscape 是否在 α=0.717 处有最小值
- 结果：v9 扫描显示 loss landscape 非凸，α=0.717 处 loss 反而不是最小
  - α=0.6 → total=2.55
  - α=0.717 → total=5.11（反而更大）
  - α=0.9 → total=1.95

**诊断分析**
- 梯度方向分析：∂loss/∂α < 0，优化器倾向增大 α，方向错误
- 采样范围 [0.4, 1.0] 可能不足，缺少球心附近信息
- 分母 Δ = (V-1)² - C² 在声速线附近数值不稳定

### 当前状态：⏸️ 暂停
后续可能需要：扩大采样范围、增加物理约束、重新检查方程实现

---

## 2. Inverse: Burgers 方程粘性系数反演

### 目标
从稀疏测量数据反推 Burgers 方程的粘性系数 ν，真实值 ν=0.1。

### 已完成工作

**代码** (`cases/Inverse/Burgers_Viscous/0.py`)
- `PINNs_Burgers_Inverse` 类：网络预测 u(x,t)，ν 作为可学习参数（log空间保证正数）
- 损失函数：1000×loss_pde + 10×loss_ic + 1×loss_data
- 测量数据：100个点，从 `measurement_data.csv` 加载
- 两阶段训练：50000 Adam + 1000 LBFGS

**实验结果**（共6次运行）
- 最新结果（20260202_155054）：
  - 初始猜测：ν = 0.05 → 反推结果：ν = 0.125 → 真实值：ν = 0.1
  - 相对误差：25.2%
  - R² = 0.977
- 流场拟合质量不错，但 ν 精度还有提升空间

### 当前状态：✅ 基本完成，可进一步优化
可尝试：调整权重比、增加测量点、不同初始猜测验证鲁棒性

---

## 3. 3D Taylor-Green Vortex

### 目标
用 PINN 求解 3D 可压缩 Navier-Stokes 方程的 Taylor-Green Vortex。
Re=1600, Ma=0.1, γ=1.4, 域 [-πL, πL]³, t∈[0, 10]。

### 已完成工作

**框架层面** (`PINNsrc/PINNs.py`)
- 新增 `PINNs_WE_NS_3D` 类，输入 (t,x,y,z) 4D，输出 (ρ,p,u,v,w) 5D
- `loss_pde`: 连续性 + 3动量（含粘性项 mu*(∇²u + 1/3∇(∇·u))）+ 能量方程
- `loss_ic`: 5个物理量的初始条件 MSE

**算例代码** (`cases/3D/TaylorGreen/TGV.py`)
- 使用 PINNsrc 框架（`from PINNs import *`）
- IC: TGV 解析初始条件，BC: 周期边界（3对面）
- 损失权重：loss_pde + 10×loss_ic + 10×loss_bc
- 采样：IC=8000, 内部=20000, BC=12000
- 网络：6层×64神经元，17285参数

**训练过程与bug修复**

1. 第一次运行：CUDA OOM
   - 80000内部点 + 128神经元 + 3D粘性项梯度太多，超出A100 40GB
   - 修复：减小到 IC=8000, 内部=20000, Nn=64

2. 第二次运行：IC loss 卡在 0.2486
   - IC tensor `reshape(-1,1)` 导致 (N,1) 与 (N,) 广播为 (N,N) 矩阵
   - 修复：去掉 `.reshape(-1,1)`，保持1D

3. 第三次运行：训练完成，最终 loss=3.26e-5

**存在问题**
- 动能 Ek 在 t=0→0.1 从 0.131 骤降到 0.002，之后几乎不变
- 物理上不对：Re=1600 的 TGV 动能应缓慢衰减，峰值耗散在 t≈9
- 原因：Nn=64 对 3D NS 五变量容量不足，网络在 t>0 输出接近零速度场
- energy_evolution.png 已改为 2×2 布局（加 log scale 子图）

### 当前状态：🔧 需要增大网络重新训练
- Nn=64 容量不足，需增大到 128 或 256
- 用户可提供多张 A100 做多卡并行
