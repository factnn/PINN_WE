# 权重函数参数 k 的自适应策略理论分析

## 1. 权重函数的作用机制

### 当前权重函数
```python
d = 1/(k*(abs(u_x)-(u_x))+1)
其中 k = 0.2 (固定值)
```

### 函数行为分析

**关键观察：**
- `abs(u_x) - u_x` 的值：
  - 当 `u_x < 0`（压缩区域，激波前）：`abs(u_x) - u_x = -2*u_x > 0`
  - 当 `u_x >= 0`（膨胀区域或均匀区）：`abs(u_x) - u_x = 0`

**权重 d 的取值：**
- 在压缩区域（`u_x < 0`）：`d = 1/(k*(-2*u_x) + 1) < 1`
  - `u_x` 越负（压缩越强），`d` 越小
  - 这意味着在激波前区域，PDE残差的权重被降低
- 在膨胀/均匀区域（`u_x >= 0`）：`d = 1`
  - 正常权重，不进行特殊处理

### 物理意义

这个权重函数的作用是：
1. **激波区域识别**：通过 `u_x < 0` 识别压缩区域（激波前）
2. **权重衰减**：在激波前降低损失权重，避免过度惩罚大梯度区域
3. **平滑过渡**：通过 `k` 控制权重衰减的敏感度

---

## 2. 参数 k 的作用

### k 对权重函数的影响

- **k 较小**（如 0.1）：
  - 权重衰减较慢，`d` 在压缩区域不会降得太低
  - 更平滑的权重分布，对激波的敏感性较低
  - 适合训练初期，网络还在学习整体解结构

- **k 较大**（如 0.5）：
  - 权重衰减较快，`d` 在强压缩区域会显著降低
  - 更尖锐的权重分布，对激波更敏感
  - 适合训练后期，需要精确捕捉激波细节

### 当前固定 k=0.2 的问题

- **训练初期**：可能过于敏感，过早降低激波区域权重，影响网络学习基本解结构
- **训练后期**：可能不够敏感，无法充分降低激波区域的权重，影响激波锐化

---

## 3. 自适应 k 的理论设计

### 设计原则

1. **训练初期**：k 应该较小，让网络先学习整体解
2. **训练中期**：k 逐渐增大，开始关注激波细节
3. **训练后期**：k 保持较大值，精确捕捉激波

### 自适应策略方案

#### 方案1：基于训练阶段（简单线性）

```python
# 第一阶段（Adam）：k 从 0.1 线性增长到 0.2
# 第二阶段（LBFGS）：k 从 0.2 线性增长到 0.5

if optimizer_type == 'Adam':
    k = 0.1 + (epoch / total_adam_epochs) * (0.2 - 0.1)
elif optimizer_type == 'LBFGS':
    k = 0.2 + (epoch / total_lbfgs_epochs) * (0.5 - 0.2)
```

**优点**：简单，易于实现  
**缺点**：不考虑实际训练状态

#### 方案2：基于损失函数值（自适应）

```python
# k 随总损失下降而增大
# 损失大时（训练初期）：k 小
# 损失小时（训练后期）：k 大

k_min, k_max = 0.1, 0.5
loss_normalized = (loss - loss_min) / (loss_max - loss_min)  # 归一化到[0,1]
k = k_max - (k_max - k_min) * loss_normalized
```

**优点**：根据实际训练状态调整  
**缺点**：需要记录损失历史，可能不稳定

#### 方案3：基于梯度统计（物理驱动）

```python
# k 根据 u_x 的统计特性自适应
# 如果 u_x 的负值很大（强激波），k 应该增大

u_x_negative = u_x[u_x < 0]  # 只考虑压缩区域
if len(u_x_negative) > 0:
    u_x_min = u_x_negative.min().item()
    # 激波越强（u_x_min 越负），k 越大
    k = 0.1 + 0.4 * torch.sigmoid(-u_x_min / threshold)
else:
    k = 0.1  # 没有激波，使用最小k
```

**优点**：物理意义明确，直接响应激波强度  
**缺点**：需要计算统计量，可能增加计算开销

#### 方案4：混合策略（推荐）

```python
# 结合训练阶段和梯度统计
# 基础k随训练阶段变化，再根据梯度统计微调

# 基础k（阶段驱动）
if optimizer_type == 'Adam':
    k_base = 0.1 + (epoch / total_adam_epochs) * 0.1  # 0.1 -> 0.2
else:
    k_base = 0.2 + (epoch / total_lbfgs_epochs) * 0.3  # 0.2 -> 0.5

# 微调（梯度驱动）
u_x_negative = u_x[u_x < 0]
if len(u_x_negative) > 0:
    u_x_min = u_x_negative.min().item()
    k_adjust = 0.1 * torch.sigmoid(-u_x_min / 10.0)  # 微调范围 ±0.1
    k = k_base + k_adjust
else:
    k = k_base
```

**优点**：平衡了阶段性和自适应性  
**缺点**：需要调参（阈值、微调范围）

---

## 4. 推荐实现方案

### 阶段1：简单线性增长（先验证可行性）

```python
# 在 loss_pde 函数中
def loss_pde(self, x, epoch=None, total_epochs=None, stage='Adam'):
    # ...
    # 计算自适应 k
    if epoch is not None and total_epochs is not None:
        if stage == 'Adam':
            k = 0.1 + (epoch / total_epochs) * 0.1  # 0.1 -> 0.2
        else:  # LBFGS
            k = 0.2 + (epoch / total_epochs) * 0.3  # 0.2 -> 0.5
    else:
        k = 0.2  # 默认值
    
    d = 1/(k*(abs(u_x)-(u_x))+1)
    # ...
```

### 阶段2：如果效果好，再考虑更复杂的自适应策略

---

## 5. 预期效果

### 训练初期（k 小）
- 网络先学习整体解结构
- 不会过早降低激波区域权重
- 损失下降更稳定

### 训练中期（k 增大）
- 开始关注激波细节
- 权重函数对激波更敏感
- 激波位置和强度逐渐准确

### 训练后期（k 大）
- 精确捕捉激波
- 激波更锐利
- 减少激波区域的过度惩罚

---

## 6. 实现注意事项

1. **k 的范围**：建议限制在 [0.05, 1.0] 之间，避免极端值
2. **平滑性**：k 的变化应该平滑，避免突然跳跃
3. **可调性**：保留固定 k 的选项，方便对比
4. **监控**：记录 k 的变化历史，便于分析

